# Genera el hash con: htpasswd -nB admin
# Copia este archivo como .env y reemplaza el valor por un hash real.
# Escapa cada '$' como '$$' para que Compose conserve el hash literalmente.
TRAEFIK_BASIC_AUTH_USERS=admin:$$2y$$REPLACE_WITH_BCRYPT_HASH

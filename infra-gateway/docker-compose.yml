services:
  traefik:
    image: traefik:v2.10
    container_name: traefik_gateway
    command:
      - "--api.dashboard=true"
      - "--api.insecure=false"
      - "--providers.docker=true"
      - "--providers.docker.exposedbydefault=false"
      - "--entrypoints.web.address=:80"
    ports:
      - "80:80"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
    networks:
      - red_observabilidad
    labels:
      - "traefik.enable=true"
      # El hash se obtiene desde .env para no guardar la credencial en Git.
      - "traefik.http.middlewares.admin-auth.basicauth.users=${TRAEFIK_BASIC_AUTH_USERS:?Define TRAEFIK_BASIC_AUTH_USERS in .env}"
      
      # 👑 EL TRUCO MAESTRO: Separamos el panel usando un subdominio falso (admin.localhost)
      - "traefik.http.routers.dashboard.rule=Host(`admin.localhost`)"
      - "traefik.http.routers.dashboard.service=api@internal"
      # Todas las peticiones al dashboard deben pasar por autenticación básica.
      - "traefik.http.routers.dashboard.middlewares=admin-auth"


networks:
  red_observabilidad:
    external: true
